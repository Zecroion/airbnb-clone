{
    "buildCommand": "npm install && npm serve build -s",

    "outputDirectory":"/build",
    "framework": "create-react-app",
    "rewrites": [
        {
            "source": "/(.*)", "destination": "/index.html"
        }
    ]
    
}