{
    "buildCommand": "npm install && npm run build -s && serve -s build",
    
    "outputDirectory":"/build",
    "framework": "create-react-app",
    "rewrites": [
        {
            "source": "/(.*)", "destination": "/index.html"
        }
    ]
    
}